# RepCount

A SwiftUI iOS app for tracking strength and cardio workouts.

## Features
- Log strength and cardio sessions
- Native SwiftUI interface

## Requirements
- Xcode
- iOS (SwiftUI)

## Running
Open `RepCount.xcodeproj` in Xcode and run on a simulator or device.
